### Exemplo de como rodar os testes no terminal
```
python3 -m unittest -v test*.py

python3 -m pytest -v test*.py
```

#### Se esta utilizando VSCode, pode se instalar plugins como:
```
Python Test Explorer for Visual Studio Code
```